export class Result {
    constructor (
        public id: string,
        public user_id: string,
        public condition_id: string,
        public condition_name: string,
        public matches: string
    ) { }
}
